#include <stdio.h>
#include <string.h>

int main(){
  printf("\n");

  char * line = NULL;
  size_t len = 0;

  getine(&line , &len , stdin);
  printf("text2 : %s\n" , text2);
  free(line);
  
  printf("\n");
}
